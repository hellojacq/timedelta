#!/usr/bin/env python
from setuptools import setup, find_packages

__version__ = "0.1.0"

setup(
    name="timedelta",
    version=__version__,
    url="https://github.com/wbbradley/timedelta",
    packages=find_packages()
)
